---
description: Mark yourself back — turn email mode OFF so request_decision uses the question tool
---

Call the `set_email_mode` tool with `mode: "off"`, then confirm in one short line that email mode is OFF and the human is at the screen again.
