---
description: Mark yourself away — turn email mode ON so request_decision may email you
---

Call the `set_email_mode` tool with `mode: "on"`, then confirm in one short line that email mode is ON and the human has left the screen.
